<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 0;
        background-color: #f4f4f4;
      }

      * {
        margin: 0;
      }
      .container {
        max-width: 500px;
        margin: auto;
        margin-top: 3em;
        padding: 20px;
        background-color: #fff;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .statusCode {
        font-size: 4em;
        color: #e60c22;
        font-weight: bolder;
      }
      .message {
        font-size: 1.2em;
        margin-top: 1em;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <p class="statusCode"><%= code %></p>
      <p class="message"><%= message %></p>
    </div>
  </body>
</html>
